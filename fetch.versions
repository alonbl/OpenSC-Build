# -*- mode: sh; -*-

[ -z "${ZLIB_VERSION}" ] && ZLIB_VERSION="1.2.3"
[ -z "${LIBTOOL_VERSION}" ] && LIBTOOL_VERSION="1.5.26"
[ -z "${OPENSSL_VERSION}" ] && OPENSSL_VERSION="SNAP-$(date -d @$(($(date +%s)-60*60*24)) +%Y%m%d)"
[ -z "${OPENSC_VERSION}" ] && OPENSC_VERSION="0.11.5"
[ -z "${LIP11_VERSION}" ] && LIP11_VERSION="0.2.4"
[ -z "${ENGINE_PKCS11_VERSION}" ] && ENGINE_PKCS11_VERSION="0.1.5"
[ -z "${CSP11_VERSION}" ] && CSP11_VERSION="0.5.0"
[ -z "${PUTTY_VERSION}" ] && PUTTY_VERSION="0.58"
